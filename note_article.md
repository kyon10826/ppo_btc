# ビットコイン売買AIを作ったら、手数料を入れた瞬間に大負けした話

> 〜 PPO（深層強化学習）で BTC 自動売買戦略を組んでわかった、教科書には載っていない 3 つの教訓 〜

---

## はじめに：いきなり結論から

5 年分のビットコイン価格データに対して、深層強化学習（PPO）で自動売買 AI を学習させました。テスト期間（約10ヶ月）の結果はこうです。

**手数料 0% の場合:**

| | 私の AI | ただ買って持つだけ |
|---|---|---|
| 10ヶ月リターン | **+112%** | +55% |
| 最大の含み損 | **-16%** | -31% |

ただ買って持っているだけの「Buy & Hold（バイ・アンド・ホールド）」を**倍以上のリターン**で倒し、しかも**含み損は半分以下**。完璧な勝利、と言いたいところですが——

**手数料 0.06%（実際の取引所と同じ水準）に変えただけで:**

| | 私の AI | ただ買って持つだけ |
|---|---|---|
| 10ヶ月リターン | **-10%** 💀 | +55% |
| 最大の含み損 | **-45%** | -31% |

**同じ AI が、手数料の数字を変えるだけで惨敗します。**

この記事は、その AI をどう作り、なぜそうなったのか、何を学べるのかを、金融も AI も初学者の方に向けて全部書いたものです。コードはリポジトリで公開しているので、興味があれば自分の手でも再現できます。

---

## このプロジェクトのゴール

やりたかったことはシンプルです。

> **「上がるか下がるか分からないビットコインを、AI に毎時 1 回ずつ売買させて、ただ持っているより上手く運用させる」**

これを **強化学習** という方法で実現します。

---

## 第 1 幕：そもそも、何をしようとしているのか

### 強化学習を「ゲームをするAI」として理解する

強化学習（Reinforcement Learning, RL）と聞くと難しそうですが、実は身近です。
**チェスや囲碁で人間を超えた AlphaGo、ファミコンを攻略する AI ——これらは全部、強化学習です。**

登場人物はたった 2 人。

- **プレイヤー（エージェント）**: 状況を見て、行動を選んで、点数（報酬）を稼ぎたい AI
- **ゲーム（環境）**: プレイヤーに状況を見せ、行動の結果を返し、点数を採点する

ループはこうです。

```
   状況 ──→ プレイヤー ──→ 行動
    ↑                      │
    └──── ゲーム ←─ 報酬 ───┘
```

これを **トレード** に当てはめてみましょう。

| ゲームAI | トレードAI |
|---|---|
| 盤面 | 価格や指標などの市場データ |
| 行動 | 買う・売る・様子見・ポジション量 |
| 報酬 | 儲け |
| 終わり | 1 試合終了 | データの終端 or 大負け |

つまり今回作るのは、**価格データという "ゲーム" をプレイして、なるべく儲けようとする AI** です。

### 売買するのは「ビットコイン本体」ではない

ここで一つ大事なことを共有させてください。今回扱うのは、ビットコインそのものではなく **「ビットコイン無期限先物」** という金融商品です。

「先物」と聞くと身構えますが、要するに **「ビットコインの値段が上がるか下がるかに賭けられる契約」** だと思ってください。普通のビットコインの取引と違うのは、

1. **本体は持たない** — 財布にビットコインは入りません。値段の動きだけを追います
2. **下がる方にも賭けられる** — 「下がる」と思ったらショート（空売り）を選べば、下落で儲かります
3. **期限がない** — いつまでも続けられて、いつでもやめられます

なぜこれを選ぶかというと、AI に **「上がる」「下がる」両方向への自由な賭けを学ばせる**ためです。普通の現物取引だと「買って待つ」しかできないので、AI の打ち手が「Buy & Hold」と同じになってしまいます。

### なぜビットコイン × 強化学習なのか

理由は 3 つ。

- 24時間365日動くので、**休場のないきれいな連続データ**が取れる
- 値動きが激しいので、**学習材料としての情報量が多い**
- 銘柄が 1 つで完結するので **題材として扱いやすい**

5 年分のデータで約 12 倍も価格が動く凶悪な市場ですが、その凶悪さこそが AI の腕の見せどころです。

---

## 第 2 幕：AI を育てる設計の旅

ここから実装の話です。コードは小さく、考え方を中心に書きます。

### 旅の地図

```
[1] データを集める
       ↓
[2] AIの「目」になる特徴量を作る
       ↓
[3] AIが遊ぶ "ゲーム盤" を組む
       ↓
[4] AIの "価値観" を報酬関数に仕込む
       ↓
[5] PPO という賢い学習法でAIを育てる
       ↓
[6] 5回のミニテストで一番強い設定を選ぶ
       ↓
[7] 本番テストで答え合わせ
```

### Step 1: データ — 43,824 個の時間スライス

Bybit 取引所が公開している、BTCUSDT 無期限先物の **1 時間足、2020-09-01 〜 2025-08-31 の 5 年分**。1 時間ごとの「始値・高値・安値・終値・出来高」、その記録が **43,824 本**。

```python
import pandas as pd
df = pd.read_pickle('data/bybit_btcusdt_perp_1h_20200901_20250831.pkl')
print(df.shape)   # (43824, 7)
```

たった 1 つの pkl ファイルから始まります。確認してみると、**欠損ゼロ、1 時間刻みで完全連続**、品質は完璧でした。仮想通貨市場が休まない恩恵です。

### Step 2: 49 個の特徴量 — AI に「市場の見方」を与える

AI に「素のローソク足」を見せても、5 年で 1 万ドル → 12 万ドルに動いた価格そのものは扱いにくい数字です。代わりに、**「価格水準に関係なく意味を持つ数値」** に変換します。

たとえばこんなふうに。

```python
df['ret_1h']   = df['close'].pct_change()       # 1時間前と比べて何%動いた?
df['ret_24h']  = df['close'].pct_change(24)     # 1日前と比べて?
df['ret_168h'] = df['close'].pct_change(168)    # 1週間前と比べて?
```

これで、価格が 1 万ドルだろうと 10 万ドルだろうと「+1%」は同じ意味を持ちます。

特徴量は全部で **14 カテゴリ・49 個** 用意しました。例えば:

| カテゴリ | 何を見るか |
|---|---|
| リターン系 | 時間幅ごとの値動き |
| ボラティリティ系 | 値動きの "暴れ具合" |
| 市場レジーム系 | トレンド相場かレンジ相場か |
| Order Flow 系 | 買い圧力と売り圧力の偏り |
| RSI / MACD | お馴染みのテクニカル指標 |
| 出来高系 | 売買の活発さ |
| 時間系 | 何時か・何曜日か（周期性） |

このうち **市場レジーム系** と **Order Flow 系** が今回の工夫。 単に値段だけでなく「**今の相場の性格**」と「**ローソク足の中で誰が強かったか**」を AI に伝える試みです。

### Step 2.5: 鬼門の "未来情報リーク" を防ぐ

ここが最大の罠です。

**「過去のバックテストでは儲かったのに、本番に出したら全然ダメ」 という話の 9 割は、未来の情報がうっかり学習に混ざる "リーク" が原因です。**

例えば「今が始まった瞬間にその時間の close を使った特徴量」を AI に渡してしまうと、AI は「未来を覗き見て」取引することになり、現実では絶対に再現できません。

防御策はシンプル。**全特徴量を 1 期ずらします。**

```python
for col in feature_cols:
    df[col] = df[col].shift(1)
df = df.dropna()
```

これだけで「時刻 *t* の AI は *t-1* 以前で確定した情報しか見られない」という制約が機械的に保証されます。地味ですが、これを忘れたら全部の数字が嘘になります。

### Step 3: 取引環境 — AI が遊ぶ "ゲーム盤" を組む

ここで強化学習らしい仕事をします。Gymnasium という API に従って、自作のゲーム盤クラスを作ります。

```python
class RiskAwareCryptoTradingEnv(gym.Env):
    def reset(self): ...                  # ゲームの最初に呼ばれる
    def step(self, action): ...           # 1 ステップ進めて結果を返す
```

ゲーム盤がやることは 3 つ。

1. **状況を AI に渡す**: 49 個の市場特徴量 + 自分の口座状態 4 つ（今のポジション・含み損益・ドローダウン・直近の勝率）= **53 個の数字**
2. **行動を受け取って、市場の値動きを反映させる**: 損益を計算し、資産を更新
3. **報酬で AI を採点する**: ← 次のステップで設計

損益の計算は驚くほどシンプルです。

```python
portfolio_return = self.position × price_return
```

**ポジション × 価格変化率**。これだけ。

- ロング（+0.5）で +1% 上昇 → +0.5%
- ショート（-0.5）で -1% 下落 → +0.5% （**マイナス × マイナス = プラス**）

「下落でも稼げる」の正体は、この符号の掛け算です。

### Step 4: 報酬関数 — AI の "価値観" を設計する

ここが本プロジェクトのコアです。

**「儲けをそのまま報酬にする」ではダメ**なんです。なぜか？ そうすると AI は **一発逆転狙いの大博打** を学んでしまうから。たまたまの大当たりを正解と認識し、再現性のない無謀な行動を学んでしまうのです。

代わりに **7 項目の合算で AI に "良い投資家の価値観" を仕込みます**。

```
報酬 = ① 基本リターン
     + ② 下落ペナルティ（負を 2 乗して罰する）
     + ③ 一貫性ボーナス（勝率が高いと褒める）
     + ④ 回復力ボーナス（含み損中の挽回を強く褒める）
     + ⑤ モメンタムボーナス（連勝を褒める）
     + ⑥ 取引ペナルティ（派手なポジション変更を罰する）
     + ⑦ 大ドローダウンペナルティ（深い含み損を強く罰する）
```

それぞれの意図はこうです。

| 項 | AI に何を伝えるか |
|---|---|
| ① | 「儲かれば嬉しい」 |
| ② | 「**でも、損失は儲けより重い**。大損は絶対にダメ」 |
| ③ | 「コツコツ勝つ姿勢を評価するよ」 |
| ④ | 「含み損になったら諦めず巻き返せ」 |
| ⑤ | 「流れに乗れたなら、降りずに乗り続けろ」 |
| ⑥ | 「気まぐれな売買は罰する」 |
| ⑦ | 「資産が大きく毀損するのは絶対避けろ」 |

これを最大化するように学習することで、AI は **「派手に儲ける」より「深い谷を作らない」を優先する性格** になっていきます。

> ☝ ここが最大のクリエイティブ作業です。報酬関数は **AI への「お手本となる価値観」の指示書**。ここに自分の投資哲学を込めるイメージ。

### Step 5: PPO — 賢く・安定して学ぶ仕組み

エージェント本体（AI の脳）は、**Stable-Baselines3** というライブラリが提供する **PPO（Proximal Policy Optimization）** を使います。自分でゼロから書く必要はありません。

PPO はざっくり言うと **「方針を少しずつ、ただし壊さないように改善していく」** アルゴリズム。OpenAI が 2017 年に発表し、安定して学習が進むので強化学習の "デフォルト選択" になっています。

```python
from stable_baselines3 import PPO
model = PPO(
    'MlpPolicy', env,
    learning_rate=2e-5,    # 慎重にゆっくり学ぶ
    gamma=0.995,           # 約 200 時間先まで見据える
    clip_range=0.15,       # 1 回の更新で方針を 15% 以上変えない
    policy_kwargs={'net_arch': [dict(pi=[1024,1024], vf=[1024,1024])]},
)
model.learn(total_timesteps=200_000)
```

中では、

1. 今の方針で実際に売買してみる（経験を集める）
2. 「予想よりどれだけ良かったか」を計算する
3. 良かった行動の確率を上げる（**ただし `clip_range` で変化を制限**）
4. 繰り返し

をひたすら回しています。この **「一度に派手に変えない」** が PPO の名前の由来（Proximal = 近接）で、トレードのようなノイズの多い世界で安定的に学べる秘密です。

### Step 6: Walk-Forward Optimization — 「過去で学んで未来で試す」を何度も

ここが論文っぽい言葉ですが、やってることは素朴です。

**「過去のデータで AI を育てて、その直後の期間でテストする」を、時間をずらしながら何度か繰り返す**

```
Split 1: 学習 [10 ヶ月]      → 検証 [次の 10 ヶ月]
Split 2: 学習 [20 ヶ月]      → 検証 [次の 10 ヶ月]
Split 3: 学習 [30 ヶ月]      → 検証 [次の 10 ヶ月]
Split 4: 学習 [40 ヶ月]      → 検証 [次の 10 ヶ月]
```

しかも今回は、**性格の違う 4 つの設定** を全部の Split で競わせます。

- **大型脳・じっくり長期** タイプ
- **超大型脳・超慎重** タイプ
- **中型脳・積極的に試行錯誤** タイプ
- **小型脳・身軽に短期判断** タイプ

合計 **4 × 4 = 16 回の独立した PPO 学習**を回し、検証期間の **Calmar 比**（後述）が最も良かった設定を採用します。

### ちょっと寄り道：Calmar 比とは

**「最大の含み損 1 単位あたり、年で何%儲けたか」** を表す指標です。

```
Calmar 比 = 年率リターン ÷ 最大の含み損
```

- 戦略 A: 年 20% / 最大下落 10% → Calmar 2.0
- 戦略 B: 年 30% / 最大下落 30% → Calmar 1.0

リターンだけ見ると B が上ですが、Calmar 比では A が上。**リターンより、痛みへの耐えやすさ** を重視する指標です。

報酬関数でも「下落を罰する」、HP 選択でも「下落を罰する」。**2 段構えで一貫してドローダウンを嫌う設計** にしているのが、このプロジェクトの統一された美学です。

### Step 7: 本番テスト — 一度も触っていないデータで答え合わせ

WFO で勝った設定で、訓練データ全体で再学習。
その後、**全工程を通じて一度も触っていない直近 10 ヶ月のデータ**（テスト期間）で動かします。

```python
test_env = RiskAwareCryptoTradingEnv(test_df, ...)
obs, _ = test_env.reset()
done = False
while not done:
    action, _ = model.predict(obs, deterministic=True)
    obs, reward, done, _, info = test_env.step(action)
```

そして「ただ最初に買って最後まで持っただけの Buy & Hold」と比べる。

---

## 第 3 幕：衝撃の結末

### 手数料 0% の場合

```
DRL 戦略 :  ████████████████ +112%   (Calmar 9.03)
Buy & Hold:  ████████          +55%   (Calmar 2.26)
```

すべての指標で AI が勝利。含み損は半分、リターンは倍。**完璧な勝利**——と言いたい。

### 手数料 0.06% の場合

```
Buy & Hold:  ████████          +55%   (Calmar 2.25)
DRL 戦略 :  ▼                  -10%   (Calmar -0.26)
```

**完敗**です。

何が起きたのか？

手数料なし版では **3,425 回** も売買していました。手数料 0.06% を 3,425 回掛けると **約 200% 相当のコスト**。これでは何をしても勝てません。

手数料あり版では取引回数が 511 回に減ったものの、それでも 511 × 0.06% = 約 30% のコストが積もります。10 ヶ月のリターンが 30% を超えなければ赤字です。

つまり、**この AI は儲かるパターンを見つけるのが上手なのに、コストを差し引くと利益が全部吹き飛ぶ程度の "薄い勝ち" しか出せていなかった**、ということです。

---

## 教訓：3 つのテイクアウェイ

### 教訓 1. 取引コストは "後から足すもの" じゃない

報酬関数も、ハイパーパラメータ選択基準も、コストゼロを前提に設計されていました。AI は「コストなんてないよ」と教わって育ったので、コストを払う前提で動けません。

**最初から報酬に取引コスト項を組み込み、選択基準も「コスト込み Calmar」にする** ——これがやり直すなら一番にやる改善点です。実運用に出すなら、絶対に外せないポイント。

### 教訓 2. 評価が良すぎたら、まずリークを疑え

このプロジェクトでは 3 段階でリークを防ぎました。

1. **特徴量**: `shift(1)` で未来を覗かせない
2. **スケーラー**: 訓練データだけで学習、検証データには適用のみ
3. **データ分割**: 検証は常に訓練の "未来"、テストは全工程で一度も触らない

これを徹底したから、手数料 0% で +112% という数字が "本物" だと信じられます。逆に、もしリーク対策が不十分なら、+200% でも +300% でも簡単に出てしまう。**机上で良すぎる結果ほど疑う**のが鉄則です。

### 教訓 3. 報酬関数が 9 割

PPO 本体はライブラリが提供してくれます。私がコードでいじったのは、結局のところ:

- **特徴量** (AI の "目")
- **報酬** (AI の "価値観")
- **データ分割** (公正な評価の仕組み)

この 3 つだけ。

そして 7 項目の報酬の **重み** をいじるだけで、AI の性格はガラッと変わります。
- 「下落ペナルティ」を強くすれば臆病な戦略になる
- 「モメンタムボーナス」を強くすればトレンドフォロー型になる
- 「取引ペナルティ」を強くすれば長期保有型になる

**報酬関数の設計は、自分の投資哲学のコード化**だと思ってください。

---

## 自分でも作ってみたい人へ

### まず必要なもの

- Python 3.10+
- pip で `gymnasium 0.29.1`, `stable-baselines3 2.3.2`, `pandas`, `numpy`, `ta`, `scikit-learn`
- GPU があると速い（CPU でも可、数時間かかります）
- 5 年分の OHLCV データ（Bybit, Binance などが公開しているものでOK）

### 改造のヒント

| やってみたいこと | 改造ポイント |
|---|---|
| 別の銘柄でやる | データを差し替えるだけでOK（ETH でも金でも） |
| 4 時間足や日足にする | データ取得粒度を変更、年率換算係数も合わせて修正 |
| 複数銘柄ポートフォリオ | 行動空間を多次元化 |
| 手数料を真面目に扱う | 報酬関数を改造 + 選択基準を「コスト込み Calmar」へ |
| もっと記憶力を持たせる | `RecurrentPPO`（LSTM 入り）に切り替え |

### 落とし穴チェックリスト

実装で気をつけるべきこと:

- [ ] `shift(1)` を忘れていないか
- [ ] Scaler を検証データで fit していないか
- [ ] バックテスト時に `deterministic=True` にしているか
- [ ] 「儲け」と「報酬」を混同していないか
- [ ] 手数料を後付けで考えていないか
- [ ] テスト期間を学習や HP 選択に紛れ込ませていないか

ひとつでもチェックが漏れると、バックテストの数字は嘘になります。

---

## おわりに

このプロジェクトを通じて一番伝えたいのは、**「うまく行く方法」より「うまく行くように見えて行かない方法」を知ることのほうが、実は価値が大きい** ということです。

手数料 0% で +112% という数字を見たとき、「お、勝った」と思って実取引に出ていたら、私の資金は溶けていたはずです。**0% と 0.06% の間に、現実と理想の谷が広がっています**。

机上の論文や記事を読んでもピンと来ないこの感覚は、自分で AI を作って数字を変えてみると、骨身で理解できます。ぜひ手を動かしてみてください。

リポジトリは公開しています。フォークして、報酬関数の重みをいじって、自分だけの "投資哲学を持った AI" を育ててみてください。失敗も含めて、たくさん学べるはずです。

---

**免責事項**

本記事およびコードは教育目的のみに提供されています。投資助言ではありません。本戦略を実取引に使用して生じた損失について筆者は一切の責任を負いません。暗号資産取引には元本を超える損失が生じる可能性があります。
